{
  "name": "@cap-js/data-privacy",
  "version": "0.5.0",
  "description": "CDS plugin providing an integration with the SAP Data Privacy Integration service for PII data retention and information retrieval based on annotations.",
  "license": "Apache-2.0",
  "author": "SAP SE (https://www.sap.com)",
  "homepage": "https://cap.cloud.sap/",
  "repository": "cap-js/data-privacy",
  "main": "cds-plugin.js",
  "keywords": [
    "cds",
    "CAP",
    "cdsbuild"
  ],
  "files": [
    "_i18n",
    "lib",
    "srv",
    "db",
    "index.cds",
    "cds-plugin.js",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "workspaces": [
    "tests/build/scenarios/app1",
    "tests/build/scenarios/app2",
    "tests/build/scenarios/app3",
    "tests/information/extend-information-endpoint",
    "tests/retention/extend-retention-endpoint"
  ],
  "dependencies": {
    "@sap/xsenv": "^6.1.0"
  },
  "peerDependencies": {
    "@cap-js/audit-logging": "^1.1.0",
    "@sap/cds": ">=9.8"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^0.4.0",
    "@cap-js/cds-types": "^0.15.0",
    "@cap-js/sqlite": "^2",
    "@eslint/js": "^10",
    "@sap/cds-common-content": "*",
    "@sap/cds-dk": "^9",
    "@sap/cds-lsp": "^9.2.1",
    "eslint": "^10",
    "jest": "^30.2.0",
    "prettier": "^3.8.1"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "npx eslint .",
    "format": "prettier . --write && format-cds -f",
    "format:check": "prettier . --check && format-cds --check",
    "test-setup": "npm run setupBookshopTest && npm run setupExtendDPIBookshop && npm run setupExtendInformationBookshop",
    "setupBookshopTest": "cd ./tests/bookshop-app && rm -rf package-lock.json && rm -rf ./node_modules/ && npm link ../../ && npm i && rm -rf ./node_modules/@sap/cds",
    "setupExtendDPIBookshop": "cd ./tests/retention/extend-retention-endpoint && npm i && rm -rf ./node_modules/@sap/cds",
    "setupExtendInformationBookshop": "cd ./tests/information/extend-information-endpoint && npm i && rm -rf ./node_modules/@sap/cds",
    "test": "npx jest --silent",
    "test:integration": "cds bind --exec -- npx jest --silent --testPathPatterns \"tests/retention/dataSubjectDeletion.test.js|tests/information/information-service.test.js\""
  },
  "jest": {
    "testTimeout": 40000
  },
  "cds": {
    "requires": {
      "sap.ilm.RetentionService": {
        "kind": "TableHeaderBlocking",
        "external": false,
        "tableRestrictions": "srv"
      },
      "sap.dpp.InformationService": {
        "external": false,
        "model": "@cap-js/data-privacy/srv/DPIInformation"
      },
      "kinds": {
        "sap.ilm.RetentionService-TableHeaderBlocking": {
          "model": "@cap-js/data-privacy/srv/TableHeaderBlocking"
        }
      }
    }
  }
}
