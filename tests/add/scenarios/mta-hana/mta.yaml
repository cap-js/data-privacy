_schema-version: "3.1"
ID: test.add.hana
version: 1.0.0
description: hana mta test app
parameters:
  enable-parallel-deployments: true
modules:
  - name: test-add-hana-srv
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    requires:
      - name: test-add-hana-db
      - name: test-add-hana-auth
resources:
  - name: test-add-hana-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: test-add-hana-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      config:
        xsappname: test-add-hana-${org}-${space}
        tenant-mode: dedicated
