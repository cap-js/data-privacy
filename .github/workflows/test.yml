name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request_target:
    branches: [main]
    types: [reopened, synchronize, opened]

permissions:
  contents: read

jobs:
  requires-approval:
    runs-on: ubuntu-latest
    name: Waiting for PR approval as this workflow runs on pull_request_target
    if: github.event_name == 'pull_request_target' && github.event.pull_request.head.repo.owner.login != 'cap-js'
    environment: pr-approval
    steps:
      - name: Approval Step
        run: echo "This job has been approved!"

  test:
    name: Tests with Node.js ${{ matrix.node-version }} (CDS ${{ matrix.cds-version }})
    runs-on: ubuntu-latest
    needs: requires-approval
    if: always() && (needs.requires-approval.result == 'success' || needs.requires-approval.result == 'skipped')
    strategy:
      fail-fast: false
      matrix:
        node-version: [22.x, 24.x]
        cds-version: [9, latest]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v6
        with:
          node-version: ${{ matrix.node-version }}
      - name: Pin CDS 9 compatible stack
        if: matrix.cds-version == '9'
        run: |
          npm pkg set "overrides.@cap-js/sqlite=^2"
          npm pkg set "overrides.@cap-js/hana=^2"
          npm pkg set "overrides.@sap/cds-mtxs=^3"
          npm pkg set "devDependencies.@sap/cds=^9"
          npm pkg set "devDependencies.@sap/cds-dk=^9"
          npm pkg set "devDependencies.@cap-js/sqlite=^2"
          npm pkg set --workspace=tests/bookshop-app "dependencies.@sap/cds=^9"
          npm pkg set --workspace=tests/bookshop-app "devDependencies.@cap-js/sqlite=^2"
          npm pkg set --workspace=tests/bookshop-app "devDependencies.@sap/cds-dk=^9"
          npm pkg set --workspace=tests/incidents-mgmt "dependencies.@sap/cds=^9"
          npm pkg set --workspace=tests/incidents-mgmt "devDependencies.@cap-js/sqlite=^2"
          npm pkg set --workspace=tests/incidents-mgmt "devDependencies.@sap/cds-dk=^9"
          npm pkg set --workspace=tests/incidents-mgmt/mtx/sidecar "dependencies.@sap/cds=^9"
          npm pkg set --workspace=tests/information/extend-information-endpoint "dependencies.@sap/cds=^9"
          npm pkg set --workspace=tests/information/extend-information-endpoint "devDependencies.@cap-js/sqlite=^2"
          npm pkg set --workspace=tests/information/extend-information-endpoint "devDependencies.@sap/cds-dk=^9"
          npm pkg set --workspace=tests/retention/extend-retention-endpoint "dependencies.@sap/cds=^9"
          npm pkg set --workspace=tests/retention/extend-retention-endpoint "devDependencies.@cap-js/sqlite=^2"
          npm pkg set --workspace=tests/retention/extend-retention-endpoint "devDependencies.@sap/cds-dk=^9"
      - run: npm i -g @sap/cds-dk@${{ matrix.cds-version }}
      - run: npm i
      - run: cds v -i
      - run: npm run test

  integration-tests:
    name: Integration Tests with Node.js ${{ matrix.node-version }} (CDS ${{ matrix.cds-version }})
    runs-on: ubuntu-latest
    needs: requires-approval
    if: always() && (needs.requires-approval.result == 'success' || needs.requires-approval.result == 'skipped')
    environment: CI
    strategy:
      fail-fast: false
      matrix:
        node-version: [22.x, 24.x]
        cds-version: [9, latest]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Integration tests
        uses: ./.github/actions/integration-tests
        with:
          CF_API: ${{ secrets.CF_API }}
          CF_USERNAME: ${{ secrets.CF_USERNAME }}
          CF_PASSWORD: ${{ secrets.CF_PASSWORD }}
          CF_ORG: ${{ secrets.CF_ORG }}
          CF_SPACE: ${{ secrets.CF_SPACE }}
          NODE_VERSION: ${{ matrix.node-version }}
          CDS_VERSION: ${{ matrix.cds-version }}
